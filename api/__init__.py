"""
api/__init__.py

Initializes the `api` package.
The API layer is a thin FastAPI application that exposes the agent
pipeline over HTTP so the frontend and external callers can trigger
analyses and retrieve reports.
"""
