# Core Google stack — pinned to avoid pip backtracking hell
google-cloud-aiplatform[agent_engines,adk]==1.101.0
google-generativeai==0.8.5

# Firestore (show bible persistence)
google-cloud-firestore==2.20.2

# API server
fastapi==0.115.12
uvicorn[standard]==0.34.3

# HTTP client (Parallel AI MCP calls)
httpx==0.28.1

# Env-var loading
python-dotenv==1.1.0
